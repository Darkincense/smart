drop table if exists BAS_CUSGRADE;

drop table if exists BAS_CUSGRADEHIS;

drop table if exists BAS_CUSGRADE_SUB;

drop table if exists BAS_CUSTYPE;

drop table if exists BAS_CUSTYPESUB;

drop table if exists BAS_CUS_DISCOUNT;

drop table if exists BAS_DIC;

drop table if exists BAS_EMP;

drop table if exists BAS_INDEX_CONFIG;

drop table if exists BAS_MENU;

drop table if exists BAS_OILDEPOT;

drop table if exists BAS_OILDEPOT_BOOK_LOG;

drop table if exists BAS_OILDEPOT_BOOK_NUM;

drop table if exists BAS_OILINFO;

drop table if exists BAS_OILINFO_DETAIL;

drop table if exists BAS_OILPRICE;

drop table if exists BAS_OILPRICEHIS;

drop table if exists BAS_OILSTOCK;

drop table if exists BAS_OILSTOCKHIS;

drop table if exists BAS_OILTYPE;

drop table if exists BAS_OIL_CHANNEL_DISCOUNT;

drop table if exists BAS_ORG;

drop table if exists BAS_ROLE;

drop table if exists BAS_ROLE_MENU;

drop table if exists BAS_SMSINFO;

drop table if exists BAS_USER;

drop table if exists BAS_USER_ROLE;

drop table if exists BIL_COMPOSE_BUY;

drop table if exists BIL_DEPOT_BUY;

drop table if exists BIL_EVALUATE;

drop table if exists BIL_GROUP_BUY;

drop table if exists BIL_INTENTION;

drop table if exists BIL_INTENTION_CUS;

drop table if exists BIL_LIMIT_BUY;

drop table if exists BIL_NOTICE;

drop table if exists BIL_NOTICESUB;

drop table if exists BIL_NOTICE_EVALUATE;

drop table if exists BIL_ORDER;

drop table if exists BIL_ORDERSUB;

drop table if exists BIL_ORDER_ONSALE_LINK;

drop table if exists BIL_ORDER_STAT_HIS;

drop table if exists BIL_OUTSTORE;

drop table if exists BIL_OUTSTOREVEHICLE;

drop table if exists BIL_PAYRECEIPT;

drop table if exists BIL_SEND;

drop table if exists BIL_SEND_STAT_HIS;

drop table if exists BIL_SEND_URGENT;

drop table if exists BIL_SURPLUS_BUY;

drop table if exists CONT_HOME_WELCOME;

drop table if exists CONT_INFO;

drop table if exists CONT_OIL_SERVICE;

drop table if exists CUS_ACCLICENCE;

drop table if exists CUS_BUSLICENCE;

drop table if exists CUS_CHELICENCE;

drop table if exists CUS_CONTACTS;

drop table if exists CUS_DRIVER;

drop table if exists CUS_EMPLINK;

drop table if exists CUS_FOCUS_OIL;

drop table if exists CUS_GASSTATION;

drop table if exists CUS_GRADE_BASE_STANDARD;

drop table if exists CUS_GRADE_EVALUE;

drop table if exists CUS_GRADE_EVALUE_HIS;

drop table if exists CUS_GRADE_JING_STANDARD;

drop table if exists CUS_GRADE_MONTH_STANDARD;

drop table if exists CUS_GROUP;

drop table if exists CUS_GROUP_LINK;

drop table if exists CUS_IDCARD;

drop table if exists CUS_INFO;

drop table if exists CUS_INVOICE;

drop table if exists CUS_LEGAL_PERSON;

drop table if exists CUS_MEMOREY_DAY;

drop table if exists CUS_OPREATION_INFO;

drop table if exists CUS_ORGCODE;

drop table if exists CUS_SALEPRICE;

drop table if exists CUS_TAXLICENCE;

drop table if exists CUS_VEHICLE;

drop table if exists CUS_WHOLICENCE;

drop table if exists INQ_CHAT_LOG;

drop table if exists INQ_COMPLAINT_ADVICE;

drop table if exists INQ_INQUIRY;

drop table if exists SERVICE_SURVEYS;

drop table if exists SERVICE_SURV_ANSWER;

drop table if exists SERVICE_SURV_ANSWER_LOG;

drop table if exists SERVICE_SURV_OPTIONS;

drop table if exists SERVICE_SURV_QUESTION;

drop table if exists 积分权重;

drop table if exists 积分消费记录;

drop table if exists 积分生成记录;

/*==============================================================*/
/* Table: BAS_CUSGRADE                                          */
/*==============================================================*/
create table BAS_CUSGRADE
(
   CUSGRADE_ID          varchar(64) not null,
   OILINFO_ID           varchar(64),
   GRADE_CODE           varchar(64) not null,
   CUSGRADE_PRICE       numeric(16,2) not null,
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (CUSGRADE_ID)
);

/*==============================================================*/
/* Table: BAS_CUSGRADEHIS                                       */
/*==============================================================*/
create table BAS_CUSGRADEHIS
(
   CUSGRADE_ID          varchar(64) not null,
   OILINFO_ID           varchar(64) not null,
   GRADE_CODE           varchar(64) not null,
   CUSGRADE_PRICE       numeric(16,2) not null,
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (CUSGRADE_ID)
);

/*==============================================================*/
/* Table: BAS_CUSGRADE_SUB                                      */
/*==============================================================*/
create table BAS_CUSGRADE_SUB
(
   CUSGRADE_SUB_ID      varchar(64) not null,
   OILINFO_ID           varchar(64),
   GRADE_CODE           varchar(64) not null,
   CUSGRADE_PRICE       numeric(16,2) not null,
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   CUSGRADE_PRICE_SUB   numeric(16,2),
   ORG_ID               varchar(64),
   primary key (CUSGRADE_SUB_ID)
);

/*==============================================================*/
/* Table: BAS_CUSTYPE                                           */
/*==============================================================*/
create table BAS_CUSTYPE
(
   CUSTYPE_ID           varchar(64) not null,
   CUSTYPE_NAME         varchar(200) not null,
   CUSTYPE_CODE         varchar(50) not null,
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int comment '0：正常
            1：删除',
   primary key (CUSTYPE_ID)
);

/*==============================================================*/
/* Table: BAS_CUSTYPESUB                                        */
/*==============================================================*/
create table BAS_CUSTYPESUB
(
   CUSTYPESUB_ID        varchar(64) not null,
   CUSTYPE_ID           varchar(64) not null,
   CUSTYPESUB_NAME      varchar(50) not null,
   CUSTYPESUB_CODE      varchar(50) not null,
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (CUSTYPESUB_ID)
);

/*==============================================================*/
/* Table: BAS_CUS_DISCOUNT                                      */
/*==============================================================*/
create table BAS_CUS_DISCOUNT
(
   DISCOUNT_ID          varchar(64) not null,
   OILINFO_ID           varchar(64) not null,
   CUS_ID               varchar(64) not null,
   EXCLUSIVE_PRICE      numeric(16,2) not null,
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   DATE_START           datetime,
   DATE_END             datetime,
   PRICE_ADD            numeric(16,2) comment '基于客户专属价上浮多少，客户经理填写。',
   primary key (DISCOUNT_ID)
);

/*==============================================================*/
/* Table: BAS_DIC                                               */
/*==============================================================*/
create table BAS_DIC
(
   DIC_ID               varchar(32) not null,
   PARENET_ID           varchar(32),
   DIC_CODE             varchar(32),
   DIC_NAME             varchar(64),
   IS_LOCK              int comment '0:否
            1:是',
   primary key (DIC_ID)
);

/*==============================================================*/
/* Table: BAS_EMP                                               */
/*==============================================================*/
create table BAS_EMP
(
   EMP_ID               varchar(64) not null,
   ORG_ID               varchar(64) not null,
   EMP_NAME             varchar(50) not null,
   EMP_PHOTO            varchar(500) comment '0：总公司
            1：分公司',
   EMP_SEX              varchar(10) not null,
   EMP_POST             varchar(100) comment '0：高级客户经理
            1：其他
            2：客户经理-专职
            3：客户经理-兼职
            4：管理人员',
   EMP_MOBILE           varchar(50),
   OFFICE_PHONE         varchar(50),
   EMP_EMAIL            varchar(200),
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (EMP_ID)
);

/*==============================================================*/
/* Table: BAS_INDEX_CONFIG                                      */
/*==============================================================*/
create table BAS_INDEX_CONFIG
(
   ID                   varchar(32) not null,
   LOGO_PATH            varchar(500),
   COMPANY_NAME         varchar(100),
   COMP_ADDRESS         varchar(100),
   COMP_POSTCODE        varchar(100),
   COMP_TEL             varchar(100),
   COMP_COPYRIGHT       varchar(200),
   OTHER                varchar(200),
   primary key (ID)
);

/*==============================================================*/
/* Table: BAS_MENU                                              */
/*==============================================================*/
create table BAS_MENU
(
   MENU_ID              varchar(32) not null comment '菜单ID',
   MENU_NAME            varchar(32) comment '菜单名称',
   MENU_URL             varchar(512) comment '菜单URL',
   PARENT_ID            varchar(32) comment '父菜单ID',
   ORDER_NO             numeric(8,0) comment '排序顺序',
   MENU_ICON            varchar(50) comment '菜单图标',
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (MENU_ID)
);

/*==============================================================*/
/* Table: BAS_OILDEPOT                                          */
/*==============================================================*/
create table BAS_OILDEPOT
(
   OILDEPOT_ID          varchar(64) not null,
   ORG_ID               varchar(64),
   DEPORT_NAME          varchar(500) not null,
   DEPORT_CODE          varchar(100),
   DEPORT_ADDRESS       varchar(500) not null,
   DEPORT_LEADER        varchar(50) not null,
   MOBILE               varchar(50) not null,
   EMAIL                varchar(50),
   DEPORT_LONGANDLAT    varchar(100),
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (OILDEPOT_ID)
);

/*==============================================================*/
/* Table: BAS_OILDEPOT_BOOK_LOG                                 */
/*==============================================================*/
create table BAS_OILDEPOT_BOOK_LOG
(
   BOOK_ID              varchar(64) not null,
   DEPOT_ID             varchar(64),
   BOOK_DATE            datetime,
   CLOCK_EIGHT_BOOK     int,
   CLOCK_TEN_BOOK       int,
   CLOCK_TW_BOOK        int,
   CLOCK_FT_BOOK        int,
   CLOCK_ST_BOOK        int,
   CREATE_ID            varchar(64),
   CREATE_DATE          datetime,
   UPDATE_ID            varchar(64),
   UPDATE_DATE          datetime,
   REMARK               varchar(500),
   DEL_FLAG             int default 0 comment '0：正常
            1：删除',
   CLOCK_EIGHT_REST     int,
   CLOCK_TEN_REST       int,
   CLOCK_TW_REST        int,
   CLOCK_FT_REST        int,
   CLOCK_ST_REST        int,
   primary key (BOOK_ID)
);

/*==============================================================*/
/* Table: BAS_OILDEPOT_BOOK_NUM                                 */
/*==============================================================*/
create table BAS_OILDEPOT_BOOK_NUM
(
   BOOK_ID              varchar(64) not null,
   DEPOT_ID             varchar(64),
   CLOCK_EIGHT_NUM      int,
   CLOCK_TEN_NUM        int,
   CLOCK_TW_NUM         int,
   CLOCK_FT_NUM         int,
   CLOCK_ST_NUM         int,
   CREATE_ID            varchar(64),
   CREATE_DATE          datetime,
   UPDATE_ID            varchar(64),
   UPDATE_DATE          datetime,
   REMARK               varchar(500),
   DEL_FLAG             int default 0 comment '0：正常
            1：删除',
   primary key (BOOK_ID)
);

/*==============================================================*/
/* Table: BAS_OILINFO                                           */
/*==============================================================*/
create table BAS_OILINFO
(
   OILINFO_ID           varchar(64) not null,
   OILTYPE_ID           varchar(64) not null,
   OILINFO_NAME         varchar(100) not null,
   OILINFO_CODE         varchar(50) not null,
   OILINFO_SPEC         varchar(50),
   OID_IMAGE            varchar(100),
   SALE_CHANNEL         int default 2 comment '0：pc
            1：app
            2：pc 和 app',
   PICKUP_PRICE         numeric(16,2) default 0,
   CHANNEL_PRICE        numeric(16,2) default 0,
   STATE                int not null comment '0：正常
            1：下架
            2：草稿箱',
   OIL_INTRODUCT        varchar(1000),
   SEND_POLICY          varchar(1000),
   SALES_SERVICE        varchar(1000),
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   PUBLIC_DATE          datetime,
   OILINFO_NAME_SHORT   varchar(32),
   OIL_ID               varchar(64),
   primary key (OILINFO_ID)
);

/*==============================================================*/
/* Table: BAS_OILINFO_DETAIL                                    */
/*==============================================================*/
create table BAS_OILINFO_DETAIL
(
   OILINFO_ID           varchar(64) not null,
   OILTYPE_ID           varchar(64) not null,
   OILINFO_NAME         varchar(100) not null,
   OILINFO_CODE         varchar(50) not null,
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   OILINFO_NAME_SHORT   varchar(32),
   IS_HOT_SALE          varchar(32),
   primary key (OILINFO_ID)
);

/*==============================================================*/
/* Table: BAS_OILPRICE                                          */
/*==============================================================*/
create table BAS_OILPRICE
(
   OILPRICE_ID          varchar(64) not null default '0',
   OILINFO_ID           varchar(64) not null,
   BASE_PRICE           numeric(16,2) default 0,
   PROVIDE_PRICE        numeric(16,2) default 0,
   MARKET_PRICE         numeric(16,2) not null default 0,
   GUIDE_PRICE          numeric(16,2) not null default 0,
   MIN_PRICE            numeric(16,2) not null default 0,
   VALID_DATE           datetime not null,
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null comment '0：正常
            1：删除',
   MAX_DISCOUNT         numeric(16,2),
   VALID_DAYS           int,
   PICKUP_DISCOUNT      numeric(16,2),
   primary key (OILPRICE_ID)
);

/*==============================================================*/
/* Table: BAS_OILPRICEHIS                                       */
/*==============================================================*/
create table BAS_OILPRICEHIS
(
   OILPRICE_ID          varchar(64) not null,
   OILINFO_ID           varchar(64) not null,
   BASE_PRICE           numeric(16,2) default 0,
   PROVIDE_PRICE        numeric(16,2) default 0,
   MARKET_PRICE         numeric(16,2) not null default 0,
   GUIDE_PRICE          numeric(16,2) not null default 0,
   MIN_PRICE            numeric(16,2) not null default 0,
   VALID_DATE           datetime not null,
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   MAX_DISCOUNT         numeric(16,2),
   VALID_DAYS           int,
   primary key (OILPRICE_ID)
);

/*==============================================================*/
/* Table: BAS_OILSTOCK                                          */
/*==============================================================*/
create table BAS_OILSTOCK
(
   OILSTOCK_ID          varchar(64) not null,
   OILDEPOT_ID          varchar(64) not null,
   OILINFO_ID           varchar(64) not null,
   OILSTOCK_NUM         numeric(18,4) not null default 0,
   OIL_DENSITY          numeric(18,4) not null,
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   UPDATE_DATE2         datetime,
   primary key (OILSTOCK_ID)
);

/*==============================================================*/
/* Table: BAS_OILSTOCKHIS                                       */
/*==============================================================*/
create table BAS_OILSTOCKHIS
(
   OILSTOCK_ID          varchar(64) not null,
   OILDEPOT_ID          varchar(64) not null,
   OILINFO_ID           varchar(64) not null,
   OILSTOCK_NUM         numeric(18,4) not null default 0,
   OIL_DENSITY          numeric(18,4) not null,
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (OILSTOCK_ID)
);

/*==============================================================*/
/* Table: BAS_OILTYPE                                           */
/*==============================================================*/
create table BAS_OILTYPE
(
   OILTYPE_ID           varchar(64) not null,
   OILTYPE_NAME         varchar(100) not null,
   PARENT_ID            varchar(64) not null,
   OILTYPE_CODE         varchar(50),
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (OILTYPE_ID)
);

/*==============================================================*/
/* Table: BAS_OIL_CHANNEL_DISCOUNT                              */
/*==============================================================*/
create table BAS_OIL_CHANNEL_DISCOUNT
(
   CHAN_DSCT_ID         varchar(64) not null,
   PC_DISCOUNT          numeric(16,2) not null,
   APP_DISCOUNT         numeric(16,2) not null,
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   PICKUP_DISCOUNT      numeric(16,2),
   primary key (CHAN_DSCT_ID)
);

/*==============================================================*/
/* Table: BAS_ORG                                               */
/*==============================================================*/
create table BAS_ORG
(
   ORG_ID               varchar(64) not null,
   ORG_NAME             varchar(200) not null,
   PARENT_ID            varchar(64) not null,
   ORG_TYPE             int not null comment '0：总公司
            1：分公司',
   ORG_ADDRESS          varchar(1000),
   ORG_LEADER           varchar(50),
   MOBILE               varchar(50),
   EMAIL                varchar(100),
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   ORG_CODE             varchar(32),
   primary key (ORG_ID)
);

/*==============================================================*/
/* Table: BAS_ROLE                                              */
/*==============================================================*/
create table BAS_ROLE
(
   ROLE_ID              varchar(32) not null,
   ROLE_NAME            varchar(32),
   ROLE_TYPE            varchar(32) comment '0：系统管理员
            1：北京销售角色
            2：客户角色',
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (ROLE_ID)
);

/*==============================================================*/
/* Table: BAS_ROLE_MENU                                         */
/*==============================================================*/
create table BAS_ROLE_MENU
(
   ROLE_MENU_ID         varchar(32) not null,
   ROLE_ID              varchar(32),
   MENU_ID              varchar(32),
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (ROLE_MENU_ID)
);

/*==============================================================*/
/* Table: BAS_SMSINFO                                           */
/*==============================================================*/
create table BAS_SMSINFO
(
   SMS_ID               varchar(64) not null,
   SMS_CONTENT          varchar(500) not null,
   RECEIVE_MOBILE       varchar(50) not null,
   RECEIVE_ID           varchar(64) comment '0：总公司
            1：分公司',
   SMS_TYPE             int not null comment '0：客户激活通知
            1：询价通知
            2：催办客户经理确认订单
            3：提醒客户付款
            4：催办客户经理转付油单
            5：配送申请已审核通过
            6：付油已全部完成，请关闭付油单(客户经理)
            7：付油已全部完成(客户)
            8:群发
            ',
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (SMS_ID)
);

alter table BAS_SMSINFO comment '记录短信信息';

/*==============================================================*/
/* Table: BAS_USER                                              */
/*==============================================================*/
create table BAS_USER
(
   USER_ID              varchar(64) not null,
   CUS_ID               varchar(64),
   LOGIN_NAME           varchar(100) not null,
   LOGIN_PASSWORD       varchar(100) comment '0：总公司
            1：分公司',
   USER_TYPE            int not null comment '0：主账户
            1：子账户
            2：管理人员(客户经理、分公司人员、总公司人员)
            ',
   STATE                int not null default 0 comment '0：已注册
            1：已激活
            2：停用',
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   LAST_LOGIN_TIME      datetime,
   LOGIN_IP             varchar(64),
   CUS_EMP_NAME         varchar(10),
   CUS_EMP_TEL          varchar(50),
   CUS_EMP_MOBILE       varchar(50),
   CUS_EMP_MAIL         varchar(100),
   CUS_EMP_DEPART       varchar(50),
   CUS_EMP_POSITION     varchar(50),
   LINK_USER            varchar(64),
   primary key (USER_ID)
);

/*==============================================================*/
/* Table: BAS_USER_ROLE                                         */
/*==============================================================*/
create table BAS_USER_ROLE
(
   USER_ROLE_ID         varchar(32) not null,
   USER_ID              varchar(64),
   ROLE_ID              varchar(32),
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (USER_ROLE_ID)
);

/*==============================================================*/
/* Table: BIL_COMPOSE_BUY                                       */
/*==============================================================*/
create table BIL_COMPOSE_BUY
(
   COMPOSE_BUY_ID       varchar(64) not null,
   START_DATE           datetime,
   END_DATE             datetime,
   SALE_CHANNEL         int comment '0：pc
            1：app
            2：pc 和 app',
   STATE                int comment '0：未开始
            1：进行中
            2：正常结束
            3：提前结束，团购有效
            4：提取结束，团购无效',
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   HOT_OIL_ID           varchar(64),
   HOT_OIL_RATIO        numeric(14,4),
   HOT_OIL_PRICE        numeric(14,4),
   HOT_OIL_MAX          numeric(14,4),
   TIED_OIL_ID          varchar(64),
   TIED_OIL_RATIO       numeric(14,4),
   TIED_OIL_PRICE       numeric(14,4),
   TIED_OIL_MAX         numeric(14,4),
   primary key (COMPOSE_BUY_ID)
);

/*==============================================================*/
/* Table: BIL_DEPOT_BUY                                         */
/*==============================================================*/
create table BIL_DEPOT_BUY
(
   DEPOT_BUY_ID         varchar(64) not null,
   OILINFO_ID           varchar(64),
   START_DATE           datetime,
   END_DATE             datetime,
   OIL_PRICE            numeric(14,4),
   DEPOT_ID             varchar(64),
   SALE_CHANNEL         int comment '0：pc
            1：app
            2：pc 和 app',
   STATE                int comment '0：未开始
            1：进行中
            2：正常结束
            3：提前结束',
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   MAX_NUM              numeric(14,4),
   primary key (DEPOT_BUY_ID)
);

/*==============================================================*/
/* Table: BIL_EVALUATE                                          */
/*==============================================================*/
create table BIL_EVALUATE
(
   BIL_EVA_ID           varchar(64) not null,
   ORDER_ID             varchar(64) not null,
   EMP_ID               varchar(64),
   SRV_ATTITUDE         numeric(12,2),
   ATTITUDE_REMARK      varchar(200),
   SRV_SPEED            numeric(12,2),
   SPEED_REMARK         varchar(200),
   SRV_PROB             numeric(12,2),
   PROB_REMARK          varchar(200),
   AVG_SCORE            numeric(12,2),
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (BIL_EVA_ID)
);

/*==============================================================*/
/* Table: BIL_GROUP_BUY                                         */
/*==============================================================*/
create table BIL_GROUP_BUY
(
   GORUP_BUY_ID         varchar(64) not null,
   OILINFO_ID           varchar(64),
   START_DATE           datetime,
   END_DATE             datetime,
   OIL_PRICE            numeric(14,4),
   GROUP_NUM            numeric(14,4),
   SALE_CHANNEL         int comment '0：pc
            1：app
            2：pc 和 app',
   STATE                int comment '0：未开始
            1：进行中
            2：正常结束
            3：提前结束，团购有效
            4：提取结束，团购无效',
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   MAX_NUM              numeric(14,4),
   primary key (GORUP_BUY_ID)
);

/*==============================================================*/
/* Table: BIL_INTENTION                                         */
/*==============================================================*/
create table BIL_INTENTION
(
   INTENTION_ID         varchar(64) not null,
   CUS_ID               varchar(64) not null,
   OILINFO_ID           varchar(64) not null,
   OILINFO_SPEC         varchar(50) comment '此处记录的为User表中的ID，为了将来可以关联到主子账户',
   OIL_NUMBER           numeric(18,4) not null,
   OIL_AMOT             numeric(16,2) not null,
   OIL_PRICE            numeric(16,2) not null comment '0：未读
            1：已读',
   STATE                int not null default 0 comment '0：意向单
            1：转订单',
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   SALE_CHANNEL         int comment '0：APP
            1：PC',
   INTENTION_TYPE       int comment '0：团购
            1：限时优惠
            2：普通意向
            3：未注册客户意向单',
   OILINFO_NAME         varchar(64),
   EXTRACT_TYPE         int,
   primary key (INTENTION_ID)
);

/*==============================================================*/
/* Table: BIL_INTENTION_CUS                                     */
/*==============================================================*/
create table BIL_INTENTION_CUS
(
   TEMP_CUS_ID          varchar(64) not null,
   CUS_COMPANY          varchar(64) not null,
   CUS_MOBILE           varchar(64) not null,
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (TEMP_CUS_ID)
);

/*==============================================================*/
/* Table: BIL_LIMIT_BUY                                         */
/*==============================================================*/
create table BIL_LIMIT_BUY
(
   LIMIT_BUY_ID         varchar(64) not null,
   OILINFO_ID           varchar(64),
   START_DATE           datetime,
   END_DATE             datetime,
   OIL_PRICE            numeric(14,4),
   SALE_CHANNEL         int comment '0：pc
            1：app
            2：pc 和 app',
   STATE                int comment '0：未开始
            1：进行中
            2：正常结束
            3：提前结束',
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   MAX_NUM              numeric(14,4),
   PAY_HOUR             numeric(14,4),
   primary key (LIMIT_BUY_ID)
);

/*==============================================================*/
/* Table: BIL_NOTICE                                            */
/*==============================================================*/
create table BIL_NOTICE
(
   NOTICE_ID            varchar(64) not null,
   CUS_ID               varchar(64) not null,
   ORDER_ID             varchar(64),
   NOTICE_CODE          varchar(50) not null,
   RESOURCE_PATH        varchar(500),
   STATE                int not null default 0 comment '0：正常
            1：关闭',
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   EVALUATE_STATE       int comment '0：未评价
            1：已评价',
   primary key (NOTICE_ID)
);

/*==============================================================*/
/* Table: BIL_NOTICESUB                                         */
/*==============================================================*/
create table BIL_NOTICESUB
(
   NOTICESUB_ID         varchar(64) not null,
   NOTICE_ID            varchar(64) not null,
   OILINFO_ID           varchar(64) not null,
   OILINFO_SPEC         varchar(50) comment '此处记录的为User表中的ID，为了将来可以关联到主子账户',
   OIL_NUMBER           numeric(18,4) not null,
   OIL_AMOT             numeric(16,2) not null,
   OIL_PRICE            numeric(16,2) not null comment '0：未读
            1：已读',
   EXTRACT_TYPE         int not null comment '0：配送
            1：自提',
   OILDEPOT_ID          varchar(64),
   EXPECT_NUM           numeric(18,4) comment '在生成配送单时，需要判断 预配送数量<数量 才允许生成配送单，同时将配送单中对应的数量累加到此字段中，如果取消配送单，需要减去相应的数量',
   COMPLETE_NUM         numeric(18,4) not null default 0 comment '当客户确认收油后，改写此值。同时判断此付油单中的所有油品是否都以配送完成，将此付油单的状态改为关闭；
            自提的油品，在出库时也要改成此值，同时判断是否可关闭此付油单',
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   OILINFO_NAME         varchar(64),
   ORDERSUB_ID          varchar(64),
   primary key (NOTICESUB_ID)
);

/*==============================================================*/
/* Table: BIL_NOTICE_EVALUATE                                   */
/*==============================================================*/
create table BIL_NOTICE_EVALUATE
(
   NOTICE_EVA_ID        varchar(64) not null,
   NOTICE_ID            varchar(64) not null,
   EMP_ID               varchar(64),
   OIL_SCORE            numeric(12,2),
   OIL_REMARK           varchar(200),
   SALER_SRV            numeric(12,2),
   SALER_REMARK         varchar(200),
   SRV_SCORE            numeric(12,2),
   SRV_REMARK           varchar(200),
   DEBT_SCORE           numeric(12,2),
   DEBT_REMARK          varchar(200),
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   OUTSTORE_ID          varchar(64),
   primary key (NOTICE_EVA_ID)
);

/*==============================================================*/
/* Table: BIL_ORDER                                             */
/*==============================================================*/
create table BIL_ORDER
(
   ORDER_ID             varchar(64) not null,
   CUS_ID               varchar(64) not null,
   ORDER_CODE           varchar(100) not null,
   PAY_TYPE             int not null comment '来源于数据字典',
   STATE                int not null default 0 comment '0：待确认
            1：待付款
            2：待转付油通知单
            3：已转付油通知单
            4：取消',
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   EVALUATE_STATE       int comment '0：未评价
            1：已评价',
   ORDER_TYPE           int comment '0：普通订单
            1：紧急购油',
   ORDERSALE_CODE       varchar(100),
   CUS_ISPAY            int not null comment '0：否
            1：是
            2：跳过',
   primary key (ORDER_ID)
);

/*==============================================================*/
/* Table: BIL_ORDERSUB                                          */
/*==============================================================*/
create table BIL_ORDERSUB
(
   ORDERSUB_ID          varchar(64) not null,
   ORDER_ID             varchar(64) not null,
   OILINFO_ID           varchar(64) not null,
   OILINFO_SPEC         varchar(50) comment '此处记录的为User表中的ID，为了将来可以关联到主子账户',
   OIL_NUMBER           numeric(18,4) not null,
   OIL_AMOT             numeric(16,2) not null,
   OIL_PRICE            numeric(16,2) not null comment '0：未读
            1：已读',
   EXTRACT_TYPE         int not null comment '0：配送
            1：自提',
   OILDEPOT_ID          varchar(64),
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   OILINFO_NAME         varchar(64),
   COMPLETE_NUM         numeric(18,4),
   primary key (ORDERSUB_ID)
);

/*==============================================================*/
/* Table: BIL_ORDER_ONSALE_LINK                                 */
/*==============================================================*/
create table BIL_ORDER_ONSALE_LINK
(
   LINK_ID              varchar(64) not null,
   ORDER_ID             varchar(64),
   ONSALE_ID            varchar(64),
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   ONSALE_TYPE          int comment '0:团购
            1:限时抢购
            2:定点优惠
            3:满量优惠
            4:组合套餐',
   primary key (LINK_ID)
);

/*==============================================================*/
/* Table: BIL_ORDER_STAT_HIS                                    */
/*==============================================================*/
create table BIL_ORDER_STAT_HIS
(
   BIL_HIS_ID           varchar(64) not null,
   ORDER_ID             varchar(64),
   STATE_TIME           datetime,
   STATE                varchar(64) comment '0：待确认
            1：待付款
            1.5：客户支付，客户经理未确认
            2：待转付油通知单
            3：已转付油通知单
            4：取消',
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (BIL_HIS_ID)
);

/*==============================================================*/
/* Table: BIL_OUTSTORE                                          */
/*==============================================================*/
create table BIL_OUTSTORE
(
   OUTSTORE_ID          varchar(64) not null,
   NOTICE_ID            varchar(64) not null,
   SEND_ID              varchar(64),
   OUTSTORE_CODE        varchar(100) not null,
   OILDEPOT_ID          varchar(64),
   OILINFO_ID           varchar(64) not null,
   OILINFO_SPEC         varchar(50) not null comment '此处记录的为User表中的ID，为了将来可以关联到主子账户',
   OUT_NUM              numeric(18,4) not null,
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   OUTSTORE_TIME        datetime,
   OIL_DENSITY          numeric(16,2),
   OIL_VOLUME           numeric(16,2),
   STORE_POSITION       varchar(100),
   HAUL_DISTANCE        numeric(16,2),
   OUT_REALNUM          numeric(18,4),
   APPLY_NUM            numeric(18,4),
   SUR_NOTICE_NUM       numeric(18,4),
   SUR_NUM              numeric(18,4),
   primary key (OUTSTORE_ID)
);

/*==============================================================*/
/* Table: BIL_OUTSTOREVEHICLE                                   */
/*==============================================================*/
create table BIL_OUTSTOREVEHICLE
(
   OUTSTOREVEHICLE_ID   varchar(64) not null,
   OUTSTORE_ID          varchar(64) not null,
   VEHICLE_CODE         varchar(50) not null,
   DRIVER_NAME          varchar(50) not null,
   DRIVER_MOBILE        varchar(50) not null,
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (OUTSTOREVEHICLE_ID)
);

/*==============================================================*/
/* Table: BIL_PAYRECEIPT                                        */
/*==============================================================*/
create table BIL_PAYRECEIPT
(
   PAYRECEIPT_ID        varchar(64) not null,
   ORDER_ID             varchar(64) not null,
   RESOURCE_PATH        varchar(500),
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (PAYRECEIPT_ID)
);

/*==============================================================*/
/* Table: BIL_SEND                                              */
/*==============================================================*/
create table BIL_SEND
(
   SEND_ID              varchar(64) not null,
   NOTICESUB_ID         varchar(64),
   SEND_CODE            varchar(100) not null,
   SEND_DATE            datetime,
   GASSTATION_ID        varchar(64) not null,
   OILINFO_ID           varchar(64) not null,
   OILINFO_SPEC         varchar(50) comment '此处记录的为User表中的ID，为了将来可以关联到主子账户',
   APPLY_NUM            numeric(18,4) not null,
   OUT_NUM              numeric(18,4) not null comment '默认为出库数量，扩展用',
   EXTRACT_TYPE         int not null default 0 comment '0：配送
            1：自提',
   OILDEPOT_ID          varchar(64),
   STATE                int not null default 0 comment '0：待确认
            1：确认
            2：已出库
            3：已签收
            4：已取消
            5：',
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   DRIVER_ID            varchar(64),
   VEHICLE_ID           varchar(64),
   TIME_INTERVAL        varchar(64),
   primary key (SEND_ID)
);

/*==============================================================*/
/* Table: BIL_SEND_STAT_HIS                                     */
/*==============================================================*/
create table BIL_SEND_STAT_HIS
(
   BIL_SEND_HIS_ID      varchar(64) not null,
   BIL_SEND_ID          varchar(64),
   STATE_TIME           datetime,
   STATE                int comment '0：待处理
            1：处理中
            2：已出库
            3：已签收
            4：已取消',
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   OP_FROM              int comment '0：客户
            1：客户经理',
   primary key (BIL_SEND_HIS_ID)
);

/*==============================================================*/
/* Table: BIL_SEND_URGENT                                       */
/*==============================================================*/
create table BIL_SEND_URGENT
(
   SEND_ID              varchar(64) not null,
   ORDER_ID             varchar(64),
   SEND_CODE            varchar(100),
   SEND_DATE            datetime not null,
   GASSTATION_ID        varchar(64) not null,
   EXTRACT_TYPE         int not null default 0 comment '0：配送
            1：自提',
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   OILDEPOT_ID          varchar(64),
   DRIVER_ID            varchar(64),
   VEHICLE_ID           varchar(64),
   primary key (SEND_ID)
);

/*==============================================================*/
/* Table: BIL_SURPLUS_BUY                                       */
/*==============================================================*/
create table BIL_SURPLUS_BUY
(
   SURPLUS_BUY_ID       varchar(64) not null,
   OILINFO_ID           varchar(64),
   START_DATE           datetime,
   END_DATE             datetime,
   OIL_PRICE            numeric(14,4),
   SURPLUS_NUM          numeric(14,4),
   SALE_CHANNEL         int comment '0：pc
            1：app
            2：pc 和 app',
   STATE                int comment '0：未开始
            1：进行中
            2：正常结束
            3：提前结束',
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   MAX_NUM              numeric(14,4),
   primary key (SURPLUS_BUY_ID)
);

/*==============================================================*/
/* Table: CONT_HOME_WELCOME                                     */
/*==============================================================*/
create table CONT_HOME_WELCOME
(
   PIC_ID               varchar(64) not null,
   PIC_PATH             varchar(500),
   PIC_ORDER            int,
   PIC_HREF             varchar(500) not null,
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   IS_STOP              int default 0 comment '0：否
            1：是',
   primary key (PIC_ID)
);

/*==============================================================*/
/* Table: CONT_INFO                                             */
/*==============================================================*/
create table CONT_INFO
(
   CONT_ID              varchar(64) not null,
   CONT_TITLE           varchar(100),
   CONT_TYPE            int comment '1:系统公告
            2：企业新闻
            3：服务政策
            4：常见问题
            5：使用帮助',
   CONT_INFO            text,
   IS_HREF              int,
   FREF_FROM            varchar(100),
   CONT_HREF            varchar(500),
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   IS_STOP              int default 0 comment '0：否
            1：是',
   primary key (CONT_ID)
);

/*==============================================================*/
/* Table: CONT_OIL_SERVICE                                      */
/*==============================================================*/
create table CONT_OIL_SERVICE
(
   PIC_ID               varchar(64) not null,
   PIC_PATH             varchar(500),
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (PIC_ID)
);

/*==============================================================*/
/* Table: CUS_ACCLICENCE                                        */
/*==============================================================*/
create table CUS_ACCLICENCE
(
   ACCLICENCE_ID        varchar(64) not null,
   CUS_ID               varchar(64) not null,
   RESOURCE_PATH        varchar(500) not null,
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   APPROVAL_NUMBER    varchar(64),
   ACCLICENCE_CODE      varchar(64),
   LEGAL_PERSON         varchar(64),
   BANK_NAME            varchar(64),
   BANK_ACCOUNT         varchar(64),
   primary key (ACCLICENCE_ID)
);

/*==============================================================*/
/* Table: CUS_BUSLICENCE                                        */
/*==============================================================*/
create table CUS_BUSLICENCE
(
   BUSLICENCE_ID        varchar(64) not null,
   CUS_ID               varchar(64) not null,
   RESOURCE_PATH        varchar(500) not null,
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   REGISTER_CODE        varchar(64),
   COMPANY_NAME         varchar(64),
   LEGAL_PERSONS        varchar(64),
   COMPANY_TYPE         varchar(64),
   ESTABLISHMENT_DATE   datetime,
   TERM_FROM            datetime,
   TERM_TO              datetime,
   primary key (BUSLICENCE_ID)
);

/*==============================================================*/
/* Table: CUS_CHELICENCE                                        */
/*==============================================================*/
create table CUS_CHELICENCE
(
   CHELICENCE_ID        varchar(64) not null,
   CUS_ID               varchar(64) not null,
   RESOURCE_PATH        varchar(500) not null comment '来源于客户类型表',
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   REGISTER_CODE        varchar(64),
   CUS_NAME             varchar(64),
   LEGAL_PERSONS        varchar(64),
   ALOW_RANGE           varchar(200),
   OPERATION_TYPE       varchar(64),
   VALID_FROM           datetime,
   VALID_TO             datetime,
   primary key (CHELICENCE_ID)
);

/*==============================================================*/
/* Table: CUS_CONTACTS                                          */
/*==============================================================*/
create table CUS_CONTACTS
(
   IDCARD_ID            varchar(64) not null,
   CUS_ID               varchar(64) not null,
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   NAME                 varchar(64),
   BIRTH                datetime,
   NATION               varchar(64),
   NATIVE_PLACE         varchar(64),
   MOBILE               varchar(64),
   OFFICE_PHONE         varchar(64),
   EMAIL                varchar(100),
   primary key (IDCARD_ID)
);

/*==============================================================*/
/* Table: CUS_DRIVER                                            */
/*==============================================================*/
create table CUS_DRIVER
(
   DRIVER_ID            varchar(64) not null,
   CUS_ID               varchar(64) not null,
   DRIVER_CODE          varchar(50) not null,
   DRIVER_OFFICEPHONE   varchar(50),
   DRIVER_MOBILE        varchar(50) not null,
   DRIVER_NAME          varchar(50) not null,
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (DRIVER_ID)
);

/*==============================================================*/
/* Table: CUS_EMPLINK                                           */
/*==============================================================*/
create table CUS_EMPLINK
(
   LINK_ID              varchar(64) not null,
   EMP_ID               varchar(64) not null,
   CUS_ID               varchar(64) not null,
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (LINK_ID)
);

/*==============================================================*/
/* Table: CUS_FOCUS_OIL                                         */
/*==============================================================*/
create table CUS_FOCUS_OIL
(
   FOCUS_ID             varchar(64) not null,
   CUS_ID               varchar(64) not null,
   OILINFO_ID           varchar(64),
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (FOCUS_ID)
);

/*==============================================================*/
/* Table: CUS_GASSTATION                                        */
/*==============================================================*/
create table CUS_GASSTATION
(
   GASSTATION_ID        varchar(64) not null,
   CUS_ID               varchar(64) not null,
   GASSTATION_NAME      varchar(200) not null comment '来源于客户类型表',
   GASSTATION_CODE      varchar(50),
   GASSTATION_LEADER    varchar(50),
   OFFICE_PHONE         varchar(50),
   MOBILE               varchar(50),
   GASSTATION_PROVINCE  varchar(100) not null,
   GASSTATION_CITY      varchar(100) not null,
   GASSTATION_AREA      varchar(100) not null,
   GASSTATION_ADDRESS   varchar(500) not null,
   GASSTATION_LONGANDLAT varchar(100),
   GASSTATION_BUILDING  varchar(100),
   STATE                int not null default 0 comment '0：正常
            1：停用',
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (GASSTATION_ID)
);

/*==============================================================*/
/* Table: CUS_GRADE_BASE_STANDARD                               */
/*==============================================================*/
create table CUS_GRADE_BASE_STANDARD
(
   FOCUS_ID             varchar(64) not null,
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   OVER_BASE_RATIO_MIN  numeric(12,4),
   OVER_BASE_RATIO_MAX  numeric(12,4),
   OVER_BASE_SCORE      numeric(12,4),
   primary key (FOCUS_ID)
);

/*==============================================================*/
/* Table: CUS_GRADE_EVALUE                                      */
/*==============================================================*/
create table CUS_GRADE_EVALUE
(
   GRADE_EVA_ID         varchar(64) not null,
   CUS_ID               varchar(64) not null,
   OIL_PLACE            varchar(64),
   QUARTER_OIL          numeric(12,4),
   OVER_BASE_RATIO      numeric(12,4),
   OVER_BASE_SCORE      numeric(12,4),
   JING_OIL             numeric(12,4),
   JING_RATIO           numeric(12,4),
   JING_SCORE           numeric(12,4),
   FIRST_OIL            numeric(12,4),
   SECOND_OIL           numeric(12,4),
   THIRD_OIL            numeric(12,4),
   MONTH_DIFF           numeric(12,4),
   MONTH_SCORE          numeric(12,4),
   CREDIT_SCORE         numeric(12,4),
   POTENTIAL_SCORE      numeric(12,4),
   SUPPORT_SCORE        numeric(12,4),
   SUPPORT_CASE         varchar(100),
   TOTAL_SCORE          numeric(12,4),
   SCORE_LEVEL          varchar(64),
   EVALUE_LEVEL         varchar(64),
   VALID_LEVEL          varchar(64),
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (GRADE_EVA_ID)
);

/*==============================================================*/
/* Table: CUS_GRADE_EVALUE_HIS                                  */
/*==============================================================*/
create table CUS_GRADE_EVALUE_HIS
(
   GRADE_EVA_HIS_ID     varchar(64) not null,
   CUS_ID               varchar(64) not null,
   OIL_PLACE            varchar(64),
   QUARTER_OIL          numeric(12,4),
   OVER_BASE_RATIO      numeric(12,4),
   OVER_BASE_SCORE      numeric(12,4),
   JING_OIL             numeric(12,4),
   JING_RATIO           numeric(12,4),
   JING_SCORE           numeric(12,4),
   FIRST_OIL            numeric(12,4),
   SECOND_OIL           numeric(12,4),
   THIRD_OIL            numeric(12,4),
   MONTH_DIFF           numeric(12,4),
   MONTH_SCORE          numeric(12,4),
   CREDIT_SCORE         numeric(12,4),
   POTENTIAL_SCORE      numeric(12,4),
   SUPPORT_SCORE        numeric(12,4),
   SUPPORT_CASE         varchar(100),
   TOTAL_SCORE          numeric(12,4),
   SCORE_LEVEL          varchar(64),
   EVALUE_LEVEL         varchar(64),
   VALID_LEVEL          varchar(64),
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (GRADE_EVA_HIS_ID)
);

/*==============================================================*/
/* Table: CUS_GRADE_JING_STANDARD                               */
/*==============================================================*/
create table CUS_GRADE_JING_STANDARD
(
   FOCUS_ID             varchar(64) not null,
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   JING_RATIO_MIN       numeric(12,4),
   JING_RATIO_MAX       numeric(12,4),
   JING_SCORE           numeric(12,4),
   primary key (FOCUS_ID)
);

/*==============================================================*/
/* Table: CUS_GRADE_MONTH_STANDARD                              */
/*==============================================================*/
create table CUS_GRADE_MONTH_STANDARD
(
   FOCUS_ID             varchar(64) not null,
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   MONTH_DIFF_MIN       numeric(12,4),
   MONTH_DIFF_MAX       numeric(12,4),
   MONTH_SCORE          numeric(12,4),
   primary key (FOCUS_ID)
);

/*==============================================================*/
/* Table: CUS_GROUP                                             */
/*==============================================================*/
create table CUS_GROUP
(
   GROUP_ID             varchar(64) not null,
   GROUP_NAME           varchar(64),
   SALER_ID             varchar(64),
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (GROUP_ID)
);

/*==============================================================*/
/* Table: CUS_GROUP_LINK                                        */
/*==============================================================*/
create table CUS_GROUP_LINK
(
   LINK_ID              varchar(64) not null,
   GROUP_ID             varchar(64),
   CUS_ID               varchar(64),
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (LINK_ID)
);

/*==============================================================*/
/* Table: CUS_IDCARD                                            */
/*==============================================================*/
create table CUS_IDCARD
(
   IDCARD_ID            varchar(64) not null,
   CUS_ID               varchar(64) not null,
   FRONT_PATH           varchar(500) comment '来源于客户类型表',
   BACK_PATH            varchar(500),
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   NAME                 varchar(64),
   ID_CARD              varchar(64),
   TERM_FROM            datetime,
   TERM_TO              datetime,
   LONG_TERM            varchar(64),
   primary key (IDCARD_ID)
);

/*==============================================================*/
/* Table: CUS_INFO                                              */
/*==============================================================*/
create table CUS_INFO
(
   CUS_ID               varchar(64) not null,
   CUS_CODE             varchar(50),
   CUS_TYPE             varchar(10),
   CUS_COMPANY          varchar(200) not null comment '0：总公司
            1：分公司',
   CUS_PROVINCE         varchar(10),
   CUS_CITY             varchar(10),
   CUS_AREA             varchar(50),
   CUS_ADDRESS          varchar(500),
   CUS_POSTCODE         varchar(50),
   CUS_LEADER           varchar(50),
   CUS_OFFICEPHONE      varchar(50) comment '用-分割    区号-电话-分机',
   CUS_EMAIL            varchar(200),
   CUS_MOBILE           varchar(50),
   CUS_GRADE            varchar(10),
   CREATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   CUS_STATE            int not null default 0 comment '0：待审核
            1：审核通过
            2：审核不通过',
   CUS_WORKSTATE        int not null comment '0：等待分配分公司
            1：等待分配客户经理
            2：待审核
            3：审核通过',
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   CUS_GRADE_TEMP       varchar(10),
   CUS_ORG              varchar(64),
   CUS_SELF_CODE        varchar(64),
   CUS_BUY_STATE        varchar(10),
   CUS_OIL_PLACE        varchar(10) comment '0:京内
            1:京外',
   CUS_QULITY_ATTACHES  varchar(200),
   IS_SMS               varchar(10),
   primary key (CUS_ID)
);

/*==============================================================*/
/* Table: CUS_INVOICE                                           */
/*==============================================================*/
create table CUS_INVOICE
(
   INVOICE_ID           varchar(64) not null,
   CUS_ID               varchar(64) not null,
   INVOICE_COD          varchar(100),
   INVOICE_BANK         varchar(100),
   INVOICE_ACCNUM       varchar(100),
   INVOICE_ADDRESS      varchar(500),
   INVOICE_OFFICEPHONE  varchar(50),
   INVOICE_NAME         varchar(200) not null,
   INVOICE_TYPE         int not null default 0 comment '0：普通发票
            1：增值税发票',
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (INVOICE_ID)
);

/*==============================================================*/
/* Table: CUS_LEGAL_PERSON                                      */
/*==============================================================*/
create table CUS_LEGAL_PERSON
(
   IDCARD_ID            varchar(64) not null,
   CUS_ID               varchar(64) not null,
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   NAME                 varchar(64),
   BIRTH                datetime,
   NATION               varchar(64),
   NATIVE_PLACE         varchar(64),
   MOBILE               varchar(64),
   OFFICE_PHONE         varchar(64),
   EMAIL                varchar(100),
   primary key (IDCARD_ID)
);

/*==============================================================*/
/* Table: CUS_MEMOREY_DAY                                       */
/*==============================================================*/
create table CUS_MEMOREY_DAY
(
   MEMO_ID              varchar(64) not null,
   CUS_ID               varchar(64) not null,
   MEMO_DAY             datetime,
   MEMO_NAME            varchar(64),
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (MEMO_ID)
);

/*==============================================================*/
/* Table: CUS_OPREATION_INFO                                    */
/*==============================================================*/
create table CUS_OPREATION_INFO
(
   OP_ID                varchar(64) not null,
   CUS_ID               varchar(64) not null,
   REMARK               varchar(500),
   HAS_VEHICLE          int,
   VEHICLE_NUM          int,
   "SENSITIVE"          varchar(500),
   LOW_SEASON           int,
   HIGH_SEASON          int,
   CNPC_RATIO           numeric(12,4),
   SINOPEC_RATIO        numeric(12,4),
   CNOOC_RATIO          numeric(12,4),
   DIESEL_YEAR_NUM      numeric(12,4),
   GAS_YEAR_NUM         numeric(12,4),
   TOTAL_NUM            numeric(12,4),
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (OP_ID)
);

/*==============================================================*/
/* Table: CUS_ORGCODE                                           */
/*==============================================================*/
create table CUS_ORGCODE
(
   ORGCODE_ID           varchar(64) not null,
   CUS_ID               varchar(64) not null,
   RESOURCE_PATH        varchar(500) not null,
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   ORG_CODE             varchar(64),
   ORG_NAME             varchar(64),
   ORG_TYPE             varchar(64),
   TERM_FROM            datetime,
   TERM_TO              datetime,
   REGIST_NUM           varchar(64),
   primary key (ORGCODE_ID)
);

/*==============================================================*/
/* Table: CUS_SALEPRICE                                         */
/*==============================================================*/
create table CUS_SALEPRICE
(
   SALEPRICE_ID         varchar(64) not null,
   OILINFO_ID           varchar(64) not null,
   CUS_ID               varchar(64) not null,
   APP_PRICE            numeric(16,2) not null,
   PC_PRICE             numeric(16,2) not null,
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (SALEPRICE_ID)
);

alter table CUS_SALEPRICE comment '自动任务计算此表   每天1次';

/*==============================================================*/
/* Table: CUS_TAXLICENCE                                        */
/*==============================================================*/
create table CUS_TAXLICENCE
(
   TAXLICENCE_ID        varchar(64) not null,
   CUS_ID               varchar(64) not null,
   RESOURCE_PATH        varchar(500) not null comment '来源于客户类型表',
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   TAX_NUM              varchar(64),
   TAX_PAYER            varchar(64),
   LEGAL_PERSONS        varchar(64),
   REGIST_TYPE          varchar(64),
   primary key (TAXLICENCE_ID)
);

/*==============================================================*/
/* Table: CUS_VEHICLE                                           */
/*==============================================================*/
create table CUS_VEHICLE
(
   VEHICLE_ID           varchar(64) not null,
   CUS_ID               varchar(64) not null,
   VEHICLE_TYPE         varchar(10) not null comment '来源于数据字典',
   VEHICLE_CODE         varchar(50) not null,
   VEHICLE_LOAD         int not null,
   VEHICLE_NAME         varchar(100) not null,
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (VEHICLE_ID)
);

/*==============================================================*/
/* Table: CUS_WHOLICENCE                                        */
/*==============================================================*/
create table CUS_WHOLICENCE
(
   WHOLICENCE_ID        varchar(64) not null,
   CUS_ID               varchar(64) not null,
   RESOURCE_PATH        varchar(500) not null comment '来源于客户类型表',
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   LICENSE_NUMBER       varchar(64),
   COMPANY_NAME         varchar(64),
   LEGAL_PERSON         varchar(64),
   OILS                 varchar(500),
   primary key (WHOLICENCE_ID)
);

/*==============================================================*/
/* Table: INQ_CHAT_LOG                                          */
/*==============================================================*/
create table INQ_CHAT_LOG
(
   LOG_ID               varchar(32) not null,
   LOG_TIME             datetime,
   LOG_CONTENT          varchar(200),
   LOG_STAT             int comment '0：未读
            1：已读',
   LOG_FROM             varchar(32),
   LOG_TO               varchar(32),
   LOG_TYPE             int comment '0:文本
            1:图片',
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   CUS_ID               varchar(64),
   primary key (LOG_ID)
);

/*==============================================================*/
/* Table: INQ_COMPLAINT_ADVICE                                  */
/*==============================================================*/
create table INQ_COMPLAINT_ADVICE
(
   CA_ID                varchar(64) not null,
   CA_TITLE             varchar(64) not null,
   CA_TYPE              varchar(64) not null comment '0:投诉
            1：建议',
   CA_CONT_TYPE         varchar(64) not null comment '油品质量
            客户经理
            油库服务
            配送服务
            提油速度
            其他',
   SEND_TIME            datetime,
   CA_CONTENT           varchar(500) not null,
   CUS_ID               varchar(64),
   CA_STATE             int comment '0：待处理
            1：处理中
            2：已关闭',
   COMPANY_CONT         varchar(500),
   COMPANY_TIME         datetime,
   SALER_CONT           varchar(500),
   SALER_TIME           datetime,
   CUS_EVALUE           varchar(64) comment '满意
            一般
            不满意',
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (CA_ID)
);

/*==============================================================*/
/* Table: INQ_INQUIRY                                           */
/*==============================================================*/
create table INQ_INQUIRY
(
   INQ_ID               varchar(64) not null,
   SENDER_NAME          varchar(64) not null,
   PHONE                varchar(64) not null,
   EMAIL                varchar(64),
   CONT_ADDRESS         varchar(200),
   SEND_TIME            datetime,
   INQ_CONTENT          varchar(1000) not null,
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (INQ_ID)
);

/*==============================================================*/
/* Table: SERVICE_SURVEYS                                       */
/*==============================================================*/
create table SERVICE_SURVEYS
(
   SURVEYS_ID           varchar(64) not null,
   TITLE                varchar(64),
   PREFACE              varchar(500),
   STATE_DATE           datetime,
   END_DATE             datetime,
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   JSON_DATA            text,
   STATUS               int,
   primary key (SURVEYS_ID)
);

/*==============================================================*/
/* Table: SERVICE_SURV_ANSWER                                   */
/*==============================================================*/
create table SERVICE_SURV_ANSWER
(
   ANSWER_ID            varchar(64) not null,
   QUEST_ID             varchar(64),
   ANSWER_CONT          varchar(500),
   USER_ID              varchar(64),
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (ANSWER_ID)
);

/*==============================================================*/
/* Table: SERVICE_SURV_ANSWER_LOG                               */
/*==============================================================*/
create table SERVICE_SURV_ANSWER_LOG
(
   LOG_ID               varchar(64) not null,
   SURV_ID              varchar(64),
   USER_ID              varchar(64),
   IS_COMPLETE          int,
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (LOG_ID)
);

/*==============================================================*/
/* Table: SERVICE_SURV_OPTIONS                                  */
/*==============================================================*/
create table SERVICE_SURV_OPTIONS
(
   OPTION_ID            varchar(64) not null,
   QUEST_ID             varchar(64),
   OPTION_NO            varchar(64),
   OPTION_TITLE         varchar(200),
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (OPTION_ID)
);

/*==============================================================*/
/* Table: SERVICE_SURV_QUESTION                                 */
/*==============================================================*/
create table SERVICE_SURV_QUESTION
(
   QUEST_ID             varchar(64) not null,
   SURVEYS_ID           varchar(64),
   QUEST_NO             int,
   QUEST_TITLE          varchar(200),
   QUEST_TYPE           int comment '0：单选
            1：多选
            2：多文本',
   CREATE_ID            varchar(64) not null,
   CREATE_DATE          datetime not null,
   UPDATE_ID            varchar(64) not null,
   UPDATE_DATE          datetime not null,
   REMARK               varchar(500),
   DEL_FLAG             int not null default 0 comment '0：正常
            1：删除',
   primary key (QUEST_ID)
);

/*==============================================================*/
/* Table: 积分权重                                                  */
/*==============================================================*/
create table 积分权重
(
   ID                   char(10) not null,
   客户行为                 char(10),
   积分值                  char(10),
   兑换比例                 char(10),
   primary key (ID)
);

/*==============================================================*/
/* Table: 积分消费记录                                                */
/*==============================================================*/
create table 积分消费记录
(
   id                   char(10) not null,
   客户ID                 char(10),
   消费时间                 char(10),
   消费积分值                char(10),
   消费用途                 char(10),
   备注                   char(10),
   删除标识                 char(10),
   primary key (id)
);

/*==============================================================*/
/* Table: 积分生成记录                                                */
/*==============================================================*/
create table 积分生成记录
(
   id                   char(10) not null,
   客户ID                 char(10),
   获得时间                 char(10),
   获得积分值                char(10),
   获得行为说明               char(10),
   备注                   char(10),
   删除标识                 char(10),
   primary key (id)
);
